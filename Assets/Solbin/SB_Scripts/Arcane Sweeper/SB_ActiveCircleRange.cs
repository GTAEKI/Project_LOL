using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SB_ActiveCircleRange : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.Alpha4)) // 범위 활성화
        {
            // 캐릭터의 근방 설치범위 활성화 (범위와는 별개로 설치 예정 위치에 다시 원이 출력)
        }
    }
}
